{
  "name": "bici-ai-voice-agent",
  "version": "1.0.0",
  "description": "AI Voice Agent System for BICI Bike Store",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd server && npm run dev",
    "client:dev": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "./render-build.sh",
    "build:alt": "cd server && npm run build && cd ../client && npm run build",
    "start": "cd server && npm start",
    "postinstall": "echo 'Dependencies will be installed by build script'"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "author": "BICI AI Team",
  "license": "MIT"
}